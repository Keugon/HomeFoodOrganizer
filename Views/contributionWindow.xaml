<Window x:Class="Essensausgleich.contributionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Essensausgleich"
        mc:Ignorable="d"
        Title="contributionWindow" Height="235" Width="382" >
    <Grid>
        <DataGrid x:Name="dGridVbeitraege" AutoGenerateColumns="False" HorizontalAlignment="Stretch" Height="NaN" VerticalAlignment="Stretch" Width="NaN">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding kategorie}" Header="Categorie"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding wert}" Header="Wert"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
