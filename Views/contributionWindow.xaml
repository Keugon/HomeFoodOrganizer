<Window x:Class="Essensausgleich.contributionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Essensausgleich.ViewModel"
        mc:Ignorable="d"
        Title="contributionWindow" Height="200" Width="180" 
        >
    
    <Grid>
        <DataGrid            
            ItemsSource="{Binding ListOfExpenses}" AutoGenerateColumns="False"
            SelectedItem="{Binding SelectedExpenseItem}"
                        >
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem
                        Command="{Binding DeleteEntry}"
                        Header="Delete"
                        />
                </ContextMenu>
            </DataGrid.ContextMenu>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kategorie" Width="100" Binding="{Binding categorie}"/>
                <DataGridTextColumn Header="Wert" Width="50" Binding="{Binding valueExpense}"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
